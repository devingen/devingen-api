package database_service

import (
	"github.com/devingen/devingen-api/model"
)

func (service DatabaseService) WorkspacesFindOfUser(base, userId string) ([]model.Workspace, error) {
	return []model.Workspace{}, nil
}
